{% extends 'front/base.html.twig' %}
{% block title %}Tâches Focus — Admin{% endblock %}
{% block page_title %}Gestion des Tâches Focus{% endblock %}

{% block content %}

{% if citation is defined %}
<div class="alert" style="background:rgba(108,99,255,0.12);border-left:3px solid #6C63FF;color:#E8E8F0;border-radius:0 12px 12px 0;">
    <p style="font-style:italic;margin:0;">💬 "{{ citation.q }}"</p>
    <p style="font-weight:700;margin:5px 0 0;color:#9A8CFF;">— {{ citation.a }}</p>
</div>
{% endif %}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h4 class="fw-bold mb-0"><i class="bi bi-check2-square me-2" style="color:#6C63FF;"></i>Tâches Focus
            <span class="badge bg-secondary ms-2">{{ tache_foci.getTotalItemCount }}</span>
        </h4>
    </div>
    <a href="{{ path('app_tache_focus_new') }}" class="btn btn-primary btn-sm">
        <i class="bi bi-plus-lg me-1"></i>Nouvelle Tâche
    </a>
</div>

<div class="card mb-4 p-3">
    <form method="GET" action="{{ path('app_tache_focus_index') }}" class="d-flex gap-2 flex-wrap align-items-end">
        <div>
            <input type="text" name="search" value="{{ search }}" placeholder="Rechercher..." class="form-control form-control-sm" style="width:200px;">
        </div>
        <div>
            <select name="statut" class="form-select form-select-sm" style="width:150px;">
                <option value="">Tous les statuts</option>
                <option value="En cours" {{ statut == 'En cours' ? 'selected' : '' }}>En cours</option>
                <option value="Non commencée" {{ statut == 'Non commencée' ? 'selected' : '' }}>Non commencée</option>
                <option value="Terminée" {{ statut == 'Terminée' ? 'selected' : '' }}>Terminée</option>
            </select>
        </div>
        <div>
            <select name="tri" class="form-select form-select-sm" style="width:150px;">
                <option value="id" {{ tri == 'id' ? 'selected' : '' }}>Trier par ID</option>
                <option value="titre" {{ tri == 'titre' ? 'selected' : '' }}>Titre</option>
                <option value="niveauDifficulte" {{ tri == 'niveauDifficulte' ? 'selected' : '' }}>Difficulté</option>
                <option value="scoreProductivite" {{ tri == 'scoreProductivite' ? 'selected' : '' }}>Score</option>
            </select>
        </div>
        <div>
            <select name="ordre" class="form-select form-select-sm" style="width:120px;">
                <option value="ASC" {{ ordre == 'ASC' ? 'selected' : '' }}>↑ Croissant</option>
                <option value="DESC" {{ ordre == 'DESC' ? 'selected' : '' }}>↓ Décroissant</option>
            </select>
        </div>
        <button type="submit" class="btn btn-primary btn-sm"><i class="bi bi-search me-1"></i>Filtrer</button>
        <a href="{{ path('app_tache_focus_index') }}" class="btn btn-outline-secondary btn-sm">Réinitialiser</a>
    </form>
</div>

{% if tache_foci|length == 0 %}
    <div class="text-center py-5 text-muted">
        <i class="bi bi-inbox display-4"></i>
        <p class="mt-2">Aucune tâche trouvée.</p>
    </div>
{% else %}
    <div class="row g-3">
        {% for tache_focu in tache_foci %}
        <div class="col-md-6 col-xl-4">
            <div class="card h-100">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-start mb-2">
                        <h6 class="fw-bold mb-0" style="color:#E8E8F0;">{{ tache_focu.titre }}</h6>
                        {% if tache_focu.statut == 'Terminée' %}
                            <span class="badge bg-success">✓ Terminée</span>
                        {% elseif tache_focu.statut == 'En cours' %}
                            <span class="badge bg-primary">● En cours</span>
                        {% else %}
                            <span class="badge bg-secondary">○ Non commencée</span>
                        {% endif %}
                    </div>
                    <p class="small mb-2" style="color:#9B9BB0;">{{ tache_focu.objectifPrincipal|slice(0,90) }}{% if tache_focu.objectifPrincipal|length > 90 %}…{% endif %}</p>
                    <div class="d-flex gap-2 flex-wrap mb-3">
                        <span class="badge" style="background:rgba(108,99,255,0.2);color:#9A8CFF;">Niv. {{ tache_focu.niveauDifficulte }}</span>
                        <span class="badge" style="background:rgba(46,204,113,0.15);color:{% if tache_focu.scoreProductivite >= 75 %}#2ECC71{% elseif tache_focu.scoreProductivite >= 50 %}#F39C12{% else %}#E74C3C{% endif %};">{{ tache_focu.scoreProductivite }}%</span>
                        {% if tache_focu.heureDebut %}
                        <span class="badge bg-secondary">{{ tache_focu.heureDebut|date('H:i') }} → {{ tache_focu.heureFin ? tache_focu.heureFin|date('H:i') : '?' }}</span>
                        {% endif %}
                    </div>
                    <div class="d-flex gap-2">
                        <a href="{{ path('app_tache_focus_show', {'id': tache_focu.id}) }}" class="btn btn-outline-primary btn-sm"><i class="bi bi-eye"></i></a>
                        <a href="{{ path('app_tache_focus_edit', {'id': tache_focu.id}) }}" class="btn btn-outline-warning btn-sm"><i class="bi bi-pencil"></i></a>
                        <form method="post" action="{{ path('app_tache_focus_delete', {'id': tache_focu.id}) }}" class="d-inline" onsubmit="return confirm('Supprimer ?')">
                            <input type="hidden" name="_method" value="DELETE">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tache_focu.id) }}">
                            <button type="submit" class="btn btn-outline-danger btn-sm"><i class="bi bi-trash"></i></button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
{% endif %}

<div class="mt-4 d-flex justify-content-center">
    {{ knp_pagination_render(tache_foci) }}
</div>

{% endblock %}