{% extends 'base.html.twig' %}

{% block title %}Edit Achievement — MindBoost{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card">
            <div class="card-body">
                <h4 class="fw-bold mb-4"><i class="bi bi-pencil-square me-2 text-warning"></i>Edit Achievement</h4>
                {{ form_start(form) }}
                <div class="mb-3">
                    {{ form_label(form.acheivementName) }}
                    {{ form_widget(form.acheivementName) }}
                    {{ form_errors(form.acheivementName) }}
                </div>
                <div class="mb-3">
                    {{ form_label(form.acheivementScore) }}
                    {{ form_widget(form.acheivementScore) }}
                    {{ form_errors(form.acheivementScore) }}
                </div>
                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-warning"><i class="bi bi-save me-1"></i>Save Changes</button>
                    <a href="{{ path('achievement_index') }}" class="btn btn-outline-secondary">Cancel</a>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
